message GCEvent {
  required double timestamp = 1;
  required Phase phase = 2;
  required EventType type = 3;

  enum Phase {
    BEGIN = 0;
    END = 1;
  }

  enum EventType {
    SCAVENGE = 0;
    MARK_SWEEP_COMPACT = 1;
    INCREMENTAL_MARKING = 2;
    PROCESS_WEAK_CALLBACKS = 3;
  }
}
